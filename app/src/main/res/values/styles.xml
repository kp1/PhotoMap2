<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/textPrimary</item>
        <item name="android:textColorSecondary">@color/textSecondary</item>
        <item name="android:textColorPrimaryInverse">@color/textPrimaryInverse</item>
        <item name="android:textColorSecondaryInverse">@color/textSecondaryInverse</item>
        <item name="android:textColorTertiaryInverse">@color/textTertiaryInverse</item>
        <item name="android:textColor">@color/abc_primary_text_material_light</item>
    </style>

    <style name="LoadProgressBar" parent="@style/Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/progressbar</item>
        <item name="android:minHeight">4dp</item>
        <item name="android:maxHeight">4dp</item>
    </style>
    <style name="SelectableItem">
        <item name="android:background">@drawable/selectable_item</item>
    </style>

    <style name="Text.Title" parent="@style/TextAppearance.AppCompat.Medium.Inverse"/>

    <style name="Text.Description" parent="@style/TextAppearance.AppCompat.Small.Inverse">
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

</resources>
